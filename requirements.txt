# requirements.txt — web-sopify (Streamlit UI)
# -> install: pip install -r requirements.txt
# -> tidak butuh GPU atau ML dependencies

streamlit>=1.35.0
requests>=2.31.0

# voice input (opsional, bisa di-skip kalau tidak dipakai)
audio-recorder-streamlit>=0.0.8
